{{define "queryFile"}}// Code generated by sqlc. DO NOT EDIT.
// source: {{.SourceName}}


module {{ .SourceName | stem | pascalCase}} 

open Npgsql
open Npgsql.FSharp
open System
open System.Data

{{ if not $.Options.EmitAutoOpenModel }}
open {{ $.Options.EmitModelName }}
{{ end }}
{{template "queryCode" . }}
{{end}}

{{define "queryCode"}}
{{range .Queries}}
{{- if $.OutputQuery .SourceName }}
let {{.ConstantName}} = {{$.Q}}-- name: {{.MethodName}} {{.Cmd}}
{{escape .SQL}}
{{$.Q}}

{{if .Arg.EmitStruct}}
type {{.Arg.Type}} = { {{- range .Arg.UniqueFields}}
  {{.Name | pascalCase }}: {{.Type | json2str }};
  {{- end}}
}
{{- end -}}

{{if .Ret.EmitStruct}}
type {{.Ret.Type}} = { {{- range .Ret.Struct.Fields}}
  {{.Name | pascalCase }}: {{.Type | json2str }};
  {{- end}}
}
{{- end -}}

{{if eq .Cmd ":one"}}
{{range .Comments}}//{{.}}
{{end}}
let {{.MethodName}} (db: NpgsqlConnection) {{if .Arg.Pair}} ({{.Arg.Pair}}) {{- end }}  =
  {{ if  .Ret.UniqueFieldsCountMultiple }}
  let reader = fun (read:RowReader) -> {
  {{- range $i, $e := .Ret.UniqueFields}}
    {{$e.Name | pascalCase }} = read.{{$e.Type | type2readerFunc }} "{{$e.Name }}"
  {{- end -}}
  }
  {{ else}}
  {{- range $i, $e := .Ret.UniqueFields}}
  let reader = fun (read:RowReader) -> read.{{$e.Type | type2readerFunc }} "{{$e.Name }}"
  {{- end -}}
  {{end}}

  db
  |> Sql.existingConnection
  |> Sql.query {{.ConstantName}}
  {{ if .Arg.NpgsqlParams -}}
  |> Sql.parameters  [ {{ .Arg.NpgsqlParams }} ]
  {{ end -}}
  |> Sql.executeRow{{if $.Options.EmitAsyncCode}}Async{{end}} reader
{{end}}

{{if eq .Cmd ":many"}}
{{range .Comments}}//{{.}}
{{end -}}
let {{.MethodName}} (db: NpgsqlConnection) {{if .Arg.Pair}} ({{.Arg.Pair}}) {{- end }} =
  {{- if eq (len .Ret.UniqueFields) 1 }}
  let reader = fun (read:RowReader) -> read.{{(index .Ret.UniqueFields 0).Type | type2readerFunc }} "{{(index .Ret.UniqueFields 0).Name}}"
  {{ else }}
  let reader = fun (read:RowReader) -> {
  {{- range $i, $e := .Ret.UniqueFields}}
    {{$e.Name | pascalCase }} = read.{{$e.Type | type2readerFunc }} "{{$e.Name}}"
  {{- end -}}
  }
  {{ end }}
  db 
  |> Sql.existingConnection
  |> Sql.query {{.ConstantName}}
  {{ if .Arg.NpgsqlParams -}}
  |> Sql.parameters  [ {{ .Arg.NpgsqlParams }} ]
  {{ end -}}
  |> Sql.execute{{if $.Options.EmitAsyncCode}}Async{{end}} reader
{{end}}

{{if eq .Cmd ":exec"}}
{{range .Comments}}//{{.}}
{{end -}}
let {{.MethodName}} (db: NpgsqlConnection) {{if .Arg.Pair}} ({{.Arg.Pair}}) {{- end }}  = 
  db 
  |> Sql.existingConnection
  |> Sql.query {{.ConstantName}}
  {{ if .Arg.NpgsqlParams -}}
  |> Sql.parameters  [ {{ .Arg.NpgsqlParams }} ]
  {{ end -}}
  |> Sql.executeNonQuery{{if $.Options.EmitAsyncCode}}Async{{end}}
{{end}}

{{if eq .Cmd ":execrows"}}
{{range .Comments}}//{{.}}
{{end -}}
let {{.MethodName}} (db: NpgsqlConnection) {{if .Arg.Pair}} ({{.Arg.Pair}}) {{- end }} =
  db 
  |> Sql.existingConnection
  |> Sql.query {{.ConstantName}}
  {{ if .Arg.NpgsqlParams -}}
  |> Sql.parameters  [ {{ .Arg.NpgsqlParams }} ]
  {{ end -}}
  |> Sql.executeNonQuery{{if $.Options.EmitAsyncCode }}Async{{end}}
{{end}}

{{if eq .Cmd ":execresult"}}
{{range .Comments}}//{{.}}
{{end -}}
let {{.MethodName}} (db: NpgsqlConnection) {{if .Arg.Pair}} ({{.Arg.Pair}}) {{- end }} =
  db 
  |> Sql.existingConnection
  |> Sql.query {{.ConstantName}}
  {{ if .Arg.NpgsqlParams -}}
  |> Sql.parameters  [ {{ .Arg.NpgsqlParams }} ]
  {{ end -}}
  |> Sql.execute{{if $.Options.EmitAsyncCode}}Async{{end}}
{{ end }}

{{- if eq .Cmd ":execlastid"}}
{{range .Comments}}//{{.}}
{{end -}}
// execlastid: implemented with SELECT lastval() after INSERT. Prefer :one with RETURNING for PostgreSQL.
let {{.MethodName}} (db: NpgsqlConnection) {{if .Arg.Pair}} ({{.Arg.Pair}}) {{- end }} =
  use cmd = new NpgsqlCommand({{.ConstantName}}, db)
{{.CommandBindParamsFSharp}}
  cmd.ExecuteNonQuery() |> ignore
  use lv = new NpgsqlCommand("SELECT lastval()", db)
  {{.ExecLastIDScalarExpr}}
{{- end }}

{{- if eq .Cmd ":batchexec"}}
{{range .Comments}}//{{.}}
{{end -}}
let {{.MethodName}} (db: NpgsqlConnection) (args: seq<{{.BatchElemType}}>) =
  use batch = new NpgsqlBatch(db)
  for arg in args do
    let bc = NpgsqlBatchCommand({{.ConstantName}})
{{.BatchBindParamsFSharp}}
    batch.BatchCommands.Add(bc) |> ignore
  batch.ExecuteNonQuery() |> ignore
{{- end}}

{{- if eq .Cmd ":batchmany"}}
{{range .Comments}}//{{.}}
{{end -}}
let {{.MethodName}} (db: NpgsqlConnection) (args: seq<{{.BatchElemType}}>) =
  let argList = args |> Seq.toList
  use batch = new NpgsqlBatch(db)
  for arg in argList do
    let bc = NpgsqlBatchCommand({{.ConstantName}})
{{.BatchBindParamsFSharp}}
    batch.BatchCommands.Add(bc) |> ignore
  use reader = batch.ExecuteReader()
  let results = System.Collections.Generic.List<list<{{.BatchResultRowType}}>>()
  {{- if eq (len .Ret.UniqueFields) 1 }}
  let readBatchRow (read: RowReader) = read.{{(index .Ret.UniqueFields 0).Type | type2readerFunc }} "{{(index .Ret.UniqueFields 0).Name}}"
  {{- else }}
  let readBatchRow (read: RowReader) = {
  {{- range $i, $e := .Ret.UniqueFields}}
    {{$e.Name | pascalCase }} = read.{{$e.Type | type2readerFunc }} "{{$e.Name}}"
  {{- end }}
  }
  {{- end }}
  for i in 0 .. argList.Length - 1 do
    let rows = System.Collections.Generic.List<{{.BatchResultRowType}}>()
    while reader.Read() do
      rows.Add(readBatchRow (RowReader(unbox reader)))
    results.Add(List.ofSeq rows)
    if i < argList.Length - 1 then
      reader.NextResult() |> ignore
  List.ofSeq results
{{- end}}

{{- if eq .Cmd ":batchone"}}
{{range .Comments}}//{{.}}
{{end -}}
let {{.MethodName}} (db: NpgsqlConnection) (args: seq<{{.BatchElemType}}>) =
  let argList = args |> Seq.toList
  use batch = new NpgsqlBatch(db)
  for arg in argList do
    let bc = NpgsqlBatchCommand({{.ConstantName}})
{{.BatchBindParamsFSharp}}
    batch.BatchCommands.Add(bc) |> ignore
  use reader = batch.ExecuteReader()
  let results = System.Collections.Generic.List<{{.BatchResultRowType}}>()
  {{- if eq (len .Ret.UniqueFields) 1 }}
  let readBatchRow (read: RowReader) = read.{{(index .Ret.UniqueFields 0).Type | type2readerFunc }} "{{(index .Ret.UniqueFields 0).Name}}"
  {{- else }}
  let readBatchRow (read: RowReader) = {
  {{- range $i, $e := .Ret.UniqueFields}}
    {{$e.Name | pascalCase }} = read.{{$e.Type | type2readerFunc }} "{{$e.Name}}"
  {{- end }}
  }
  {{- end }}
  for i in 0 .. argList.Length - 1 do
    if not (reader.Read()) then
      failwith "batchone: expected one row"
    results.Add(readBatchRow (RowReader(unbox reader)))
    if i < argList.Length - 1 then
      reader.NextResult() |> ignore
  List.ofSeq results
{{- end}}

{{- if eq .Cmd ":copyfrom"}}
{{range .Comments}}//{{.}}
{{end -}}
let {{.MethodName}} (db: NpgsqlConnection) (args: seq<{{.Arg.Type}}>) =
  use writer = db.BeginBinaryImport({{printf "%q" .CopyFromSQL}})
  for arg in args do
    writer.StartRow()
    {{- range .Arg.CopyFromFields}}
    {{- if isOption .Type}}
    match {{fieldAccessor "arg" .}} with
    | Some value -> writer.Write(value)
    | None -> writer.WriteNull()
    {{- else}}
    writer.Write({{fieldAccessor "arg" .}})
    {{- end}}
    {{- end}}
  writer.Complete()
{{- end -}}

{{ end }}
{{ end }}
{{ end }}


